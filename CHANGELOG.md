# 变更记录（Changelog）

本文件记录 KK 物联云的主要变更。日期为提交日期（非语义化版本发布日期）。

## 2026-08-04

### 功能
- 控制台 UI/UX 优化与只读权限收敛（`0a9ee3e`）

### 修复
- WebSocket 推送单实例重复：本地直推 + Redis 回环各推送一次（`e0c7252`）

## 2026-08-03

### 新增
- 一/二级账号体系 + 产品下放 + 实时 fan-out（`3230e74`）
- 账号内 RBAC：只读账号（operate / view）（`f08a3fb`）
- VitePress 开发者文档站（`73cd60d`）

### 功能
- 用户修改登录密码（`3254ac7`）

### 修复
- `PrimaryAuth` 放行平台超管（E2E 发现）（`b146fda`）
- 物模型校验键名（`type` → `dataType`）；生产 Redis 健康检查环境变量（`2578d34`）

### 其它
- 商业化：清除文档与代码中的第三方平台提及及实现来源措辞（`3720736`）

## 2026-08-02

### 新增 / 阶段六
- 多实例水平扩展（WebSocket 扇出、Modbus 分布式锁、TCP 下行跨实例路由）+ EMQX 规则引擎集成（`128f531`）
- 全 Docker Compose 生产套件 + 可配置管理员密码 + OTA 下载修复（`7194cff`）

### 功能
- 完美化升级：性能优化 + 协议增强 + 规则引擎 + 开发者工具 + 前端完善 + OTA（`b3f7733`）
- MQTT / TCP 全面对标三大平台（有人 / 天翼 / OneNET）（`23546f9`）

### 文档
- 重写 README（全面对标实现细节）+ 新增 CLAUDE.md（`509c6d7`）
- CLAUDE.md 增加测试服务器（阿里云 ECS）部署说明与薄镜像流程（`ec485e6`）

## 2026-07-30

### 功能
- Modbus 轮询引擎升级：分组分频 + 寄存器批量读取 + 变更上报（`989055f`）
- 产品工作台对齐天翼 + 界面优化（`7c843a6`）

## 2026-07-29

### 新增 / 初始版本
- 四期：物模型 TSL 升级 + 产品接入模式 + Modbus 云端轮询（`052337c`）
- 产品创建独立页面 + 接入方式互斥约束 + 平台更名 KK 物联云（`4a4511e`）
- 初始提交：物联云平台一至三期完整功能（`dc9f794`）

---

> 变更类型约定：**新增**（新功能）／**功能**（功能改进）／**修复**（缺陷修复）／**文档**（仅文档）／**部署**（部署相关）。括号内为提交短哈希，可用 `git show <hash>` 查看详情。
